{
  "name": "workspace-fmteam",
  "version": "1.0.0",
  "description": "Sistema de gestão de tempo - Frontend + Backend",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "gunicorn --bind 0.0.0.0:$PORT main:application"
  },
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.9.0"
  }
}